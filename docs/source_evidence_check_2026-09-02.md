# 来源证据核对记录（2026-09-02）

## 目的与边界

本记录是对正式 Demo 固定题库所引用资料的**逐条证据核对**：检查链接、版本、定位和题目正确答案是否能由来源直接支持。

截至 2026-09-02，这还不是人的最终签字；当时所有条目的 `verified` 和来源台账中的 `review_status` 均保持原值，仍待负责人填写姓名、日期、结论和授权范围。现场操作必须以目标设备手册、现行法规、风险评估和现场规程为准。

> 后续状态：项目负责人 xyh202131 已于 2026-09-03 完成 20 条来源的最终人工
> 复核。签字结果见 [`data/demo_source_manifest.json`](../data/demo_source_manifest.json)，
> 可提交的原文片段及哈希见 [`data/sources/index.json`](../data/sources/index.json)。
> 本文以下内容仍保留为 2026-09-02 的机器核对历史记录。

## 本轮已修正

- KB-003、KB-004：原资料实际是 `Controller S4Cplus Automotive M2000`，不是 IRB 340 机械臂手册；已更正标题和适用范围。
- KB-009、KB-026：改用直接列出 INFORM `MOVJ`、`MOVL`、`MOVC` 语义的 Yaskawa 官方页面；删去不能由原文直接支持的通用节拍优化结论。
- KB-024：旧 ABB 链接已不可访问；已换为可访问的 `3HAC050947-001 Rev.X`，定位到第 145 页。
- Q-002、Q-016、Q-024、Q-027、Q-046、Q-047：已改为来源原文可直接回答的表述，避免把现场经验、跨型号要求或推论当作来源事实。

## 逐条结果

| source_id | 核对结果 | 可定位证据与适用边界 |
| --- | --- | --- |
| KB-001 | 证据匹配 | Yaskawa 运动入门页的 `The Robot’s Joints` 段：标准六轴机器人有六个关节，前三轴控制工具位置、后三轴控制姿态。仅限页面所述的典型系统。 |
| KB-002 | 证据匹配 | 同页的 `Controller` 段：控制柜包含计算机、电力电子和电机驱动器，运行程序并向电机发送命令。 |
| KB-003 | 证据匹配 | ABB `3HAC 17710-1 Rev.3`，§1.2.1，第 10 页；三位使能装置在松开或完全按死时会停止移动。仅限 S4Cplus Automotive M2000 控制器。 |
| KB-004 | 证据匹配 | 同一 ABB 手册；手动/自动模式和手动最高 250 mm/s 的陈述可定位。不能外推为所有 ABB 控制器模式。 |
| KB-005 | 证据匹配 | ABB `3HAC16581-1 Rev.J`，`SToolTCPCalib`，第 507–509 页；位置和运动关联工具坐标系，强调正确的工具坐标定义。 |
| KB-006 | 证据匹配 | 同一 ABB 手册；四个姿态不同的点、不同方向和标定质量检查均可定位。 |
| KB-007 | 证据匹配 | ABB `4GAA412009915-001 Rev.C`，§4.2.5.1，第 75 页；明确建议四点法并在后续验证 TCP 精度。仅限该安装手册的校准笔场景。 |
| KB-008 | 证据匹配 | Yaskawa `HW1484476`，§5.1，第 55/57 页；`THREE STEPS SAME LINE` 对应三点共线并要求使用不共线三点重教。 |
| KB-009 | 证据匹配 | Yaskawa `Programming Languages & Developer Tools` 的 `INFORM > Common Instructions`；`MOVJ`、`MOVL`、`MOVC` 的定义可直接对应题目。 |
| KB-011 | 证据匹配 | ABB `3HAC050917-001 Rev.F`，`zonedata`，第 1733–1738 页；`fine`、飞越点和 `z1` 的预定义数据均可定位。 |
| KB-012 | 证据匹配 | Yaskawa 编程工具说明的 `Language Characteristics / Common Instructions`；行式语法、流程控制和 `CALL JOB:name` 可直接对应题目。 |
| KB-013 | 证据匹配 | 同一 Yaskawa 页面；`DOUT OT#(n) ON/OFF`、`WAIT IN#(n)=ON` 及 PLC/外围设备 I/O 握手说明可定位。 |
| KB-017 | 证据匹配 | FANUC MyPortal 注册页列出 `Alarm Code Lookup`；题目已收窄为该页面明确列出的功能，不再声称页面给出所有报警的通用处置步骤。 |
| KB-019 | 证据匹配，但全文需登录 | FANUC Tech Transfer 的公开摘要给出“断电时未移动”“版本要求”“校对后验证精度”等前提；完整教学视频需要登录，不可替代机型手册。 |
| KB-020 | 证据匹配 | Yaskawa GP12 维护页的 S/L/U 轴步骤 3–6；未拆排脂塞时加脂可能损坏电机或油封。仅限列明的 GP12 型号。 |
| KB-021 | 证据匹配，历史资料 | OSHA `STD 01-12-002` 附录 A 的维护条目 8–9；要求遵循制造商预防性维护计划和周期检查安全关键设备。页面为历史档案，不是现行法规替代品。 |
| KB-022 | 证据匹配，历史资料 | 同一 OSHA 附录 A 的 `Interlocked Barrier Guard`；开门时停止自动运行，关闭门后还需从隔离区外重新使能。页面为历史档案。 |
| KB-023 | 证据匹配，历史资料 | 同一 OSHA 附录 A 的教学模式与培训条目；限定进入人员、低速、危险位置规避和急停培训。页面为历史档案。 |
| KB-024 | 证据匹配 | ABB `3HAC050947-001 Rev.X`，§2.6，第 145 页；模型化碰撞/卡滞/负载定义检测、负载辨识和触发后的停止均可定位。 |
| KB-026 | 证据匹配 | 与 KB-009 同一 Yaskawa 原文；仅保留 `MOVJ`、`MOVL`、`MOVC` 的指令语义，不再把它包装为通用节拍优化规则。 |

## 负责人最终签字结果（2026-09-03）

1. 20 条正式 Demo 来源全部通过人工复核，复核人 `xyh202131`。
2. 两份来源台账均已填写结论、引用边界、仓库片段路径及 SHA-256。
3. 知识库相应记录已发布为 `verified: true`；缺片段或哈希不一致时程序会失败关闭。
4. 厂商型号、历史 OSHA 文件、需登录内容等适用边界继续保留，不因通过复核而扩大。
