# 产品落地实施与验收计划

> 状态：执行基线；总负责人：肖云涵。
> 本计划把可运行研究原型收敛为可复验的产品候选。计划、模板和工作稿不是完成证据；未实际形成的 PPT、视频、盖章报名表、外部提交回执和正式 scorecard 均保持未完成状态。

## 三类读者的交接入口

- **开发者**：保持现有在线闭环可运行，按 [README](../README.md) 完成部署与回归；不得把内部规则评测说成独立正式指标。
- **验收负责人**：按本计划的 M0–M6、G0–G4、UAT 和回滚规则收集证据；对每次变更建立新的候选 SHA 和冻结数据，不覆盖历史材料。
- **评审者**：从[官方任务书落地矩阵](官方任务书落地矩阵.md)核对官方条款、责任与缺口，从[官方指标数学口径与验收](官方指标数学口径与验收.md)复算 G2 指标，并仅把已实际登记的外部材料视为事实。

## 范围、依赖与关键路径

本阶段交付工业机器人领域的多智能体学习闭环、部署说明、单元测试、证据矩阵和正式指标复算通道。它不新增第二领域、生产鉴权、数据库、多租户或移动端；官方任务书第 8 页将测试用例不足 50 组列为方案不完整的扣分条件，因此以 `>=50` 为完整性边界（非单独明示的满分档），不将 300+ 知识点、双人盲评、10,000 次 case-cluster Bootstrap、保守 CI 和证据状态机等内部加强冒充为官方原文。

关键路径为：

```text
M0 要求/口径冻结 -> M1 工具可运行 -> M2 独立盲评包 -> M3 正式指标冻结
-> M4 材料一致化 -> M5 候选包复验 -> M6 只提交不开发
```

M2 的两名独立领域评分者和 M3 的第三位仲裁者是外部人工依赖；M5 的报名表、M6 的报名/邮件/云盘回执也是外部依赖。系统和开发者可验证格式、计算和文件哈希，但**不得**替代评分者、来源复核者、仲裁者、签字人、报名系统或赛事方。独立角色均从 `unassigned` 开始，须由肖云涵实际指定并保留授权/回避记录。

## M0–M6 时间线、分工和每里程碑证据

| 里程碑与时间 | 前置依赖与负责人 | 交付和验收动作 | 必须保留的证据 | 当前边界 |
|---|---|---|---|---|
| M0，9 月 3 日上午：要求与口径冻结 | 官方任务书；肖云涵 | 确认条款逐行映射、数学口径、内部/官方边界和候选分支。 | `docs/官方任务书落地矩阵.md`、本计划、数学口径、G0 索引条目、决策记录。 | 工作稿不等于人工签核或外部回执。 |
| M1，9 月 3 日下午：计分工具可运行 | M0；肖云涵（实现） | 运行 `formal_scorecard` 测试、模板错误路径、证据索引校验。 | 真值模板、评分器、单测、CLI 命令日志、G2 working 条目。 | 空模板应为 `not_assessable`，不产生赛事结果。 |
| M2，9 月 3 日晚：盲评包可发放 | M1、冻结候选输出；肖云涵组织，两名独立领域评分者 | 冻结 `>=50` 用例、`>=3` 画像、输出、覆盖全集与 version-1 manifest；为两位评分者生成彼此隔离的材料。 | 候选 SHA、数据集 ID、repository/manifest/content 哈希、profile/case ownership 与 50 个唯一 hash-pair 检查、评分分配与盲化记录、两份评分表、仲裁模板。 | 未具名/未签字的模板不可提升为正式真值。 |
| M3，9 月 4 日上午：正式指标冻结 | M2、第三位仲裁者；评分者、仲裁者、肖云涵 | 完成分歧仲裁，冻结真值，运行 CLI，复核退出码和产物。 | `formal_truth.json`、canonical manifest hash、`scorecard.json`、`scorecard.md`、共同 pair/Kappa 记录、仲裁日志、外部签字/授权记录。 | 缺任何人工、manifest/ownership 或数据质量门禁即 `not_assessable`；仅实际签字批准的报告可登记为候选/批准。 |
| M4，9 月 4 日下午：材料一致化 | M3；肖云涵 | 用同一候选 SHA 和同一正式 scorecard 更新唯一技术 PDF、作品介绍、PPT、视频脚本与录制清单。 | PDF、PPT、视频、脚本、版本交叉核对表。 | 当前这些外部材料尚未完成，不能写为已交付。 |
| M5，9 月 4 日晚：候选包复验 | M4、报名表；肖云涵 | 在清洁环境部署、执行 UAT、生成 SHA-256，核对包内版本一致。 | 提交包、`CHECKSUMS.sha256`、清洁部署记录、UAT 记录、报名表副本（若实际完成）。 | 报名表/盖章尚属外部人工材料，不能由仓库生成或伪造。 |
| M6，9 月 5 日：只提交不开发 | M5；肖云涵 | 停止功能改动，只提交经复验的包并保存送达证据。 | 邮件/云盘时间戳、收件/上传回执、审核状态截图或记录。 | 实际提交、报名、审核和赛事结果均由外部主体决定。 |

资源分工：肖云涵负责范围控制、候选版本、部署、证据登记、材料一致性和提交；实现侧 `xyh202131` 维护真值契约、评分器、测试和文档；独立领域评分者完成盲评；独立仲裁者裁决分歧；来源/合规复核者核验来源与数据边界。任何一个独立审阅或裁决职位都不能由系统、模型、自动脚本或同一开发者兼任。

## G0–G4 门禁

| 门禁 | 目标 | 通过条件 | 主要证据 | 不通过时 |
|---|---|---|---|---|
| G0 要求与版本 | 条款可追溯且范围正确 | 任务书逐项映射，官方与内部强化分列，候选 SHA 明确。 | 落地矩阵、设计/计划、`EV-G0-*`。 | 回到 M0；不得用计划替代条款完成。 |
| G1 在线闭环 | 可部署并完成画像到反馈再生成的路径 | 服务启动，三类画像、资源生成、可视化、反馈链路和回归测试可复验。 | README 命令、测试日志、运行记录/截图。 | 回滚到上一个可运行 SHA，修复后重跑 UAT。 |
| G2 正式指标与可信度 | 独立真值可复算 | 真值契约、`>=50` case、`>=3` profile、version-1 manifest/hash/ownership、盲评、第三人仲裁、每类共同有效 pair 与 Kappa、三项保守门禁均符合数学口径。 | 冻结真值、repository/manifest 哈希、scorecard JSON/MD、签字/分配/仲裁记录。 | `not_assessable` 或 `fail`；保留报告并新建冻结版本，不能选择性重算。 |
| G3 展示材料 | 材料只引用同一事实版本 | PDF、PPT、10 分钟以内视频和脚本与候选 SHA、G2 结论一致。 | 实际文件与交叉核对表。 | 停止封包，回到 M3/M4 修订；未生成材料只登记为 `planned`，并在 `limitations` 和 `visibility` 写明外部/缺失边界。 |
| G4 封包与外部提交 | 提交件完整、可定位、真实送达 | 清洁部署、UAT、哈希、报名与送达记录齐全。 | 包、校验和、报名表、邮件/云盘回执。 | 不伪造回执或盖章；未形成材料只登记为 `planned`，并在 `limitations` 和 `visibility` 写明外部/缺失边界。 |

## UAT 用例与验收记录

每次候选包复验都记录执行人、日期、候选 SHA、环境、输入、预期、实际、截图/日志路径和结论。

| 用例 | 操作 | 预期与证据 |
|---|---|---|
| UAT-01 清洁启动 | 按 README 配置或清空 API key 后启动 `server.py`，打开 `http://127.0.0.1:8000`。 | 首页可访问，离线桩可保持闭环；记录启动日志。 |
| UAT-02 三类画像 | 依次选择三份合成画像，完成诊断和资源生成。 | 每个画像均显示个性化路径、三类资源和决策可视化；保存三次记录。 |
| UAT-03 反馈再生成 | 在任一画像完成一次学习反馈后继续流程。 | 系统展示反馈后的决策理由及更新资源；保存前后对照。 |
| UAT-04 回归 | 按下方 Windows PowerShell 命令清空 provider key 后运行 Python 全量测试、前端测试、模板 smoke 与证据索引校验。 | Python、Node 和索引命令退出 0；模板 smoke 的评分器退出 2 且两份报告存在；失败不进入 G3/G4。 |
| UAT-05 模板错误路径 | 使用空模板运行 `formal_scorecard`。 | 退出码为 2，生成 JSON/Markdown，状态为 `not_assessable`；不登记为正式成绩。 |
| UAT-06 正式复算 | 仅在实际存在的冻结真值上运行 scorecard。 | 两份报告、repository/manifest 哈希、ownership/共同 pair 门禁和退出码与 M3 记录一致；无真值则本项未完成。 |

## 回滚和异常规则

1. G1 回归、UAT 或部署失败：停止进入后续门禁，回退到最近一个已通过 UAT 的候选 SHA；将失败日志链接到决策记录，不删除失败证据。
2. G2 输入、盲化、签字、仲裁、Kappa 或样本门禁失败：保持 `not_assessable`，不修改旧冻结文件；建立新 dataset ID，重新盲评和仲裁。
3. G2 指标为 `fail`：保留 scorecard 与事实数据；修复产品后新建候选 SHA 和完整新样本，不得仅替换不利记录或重跑直到通过。
4. G3 文档、PPT 或视频与 scorecard 不一致：冻结封包，回到 M3/M4；只允许用同一份实际批准的证据更新材料。
5. G4 外部提交、报名或盖章缺失：不伪造文件，不标记完成；在证据索引只保留 `planned`，并通过 `limitations` 与 `visibility` 说明外部依赖和实际缺口，等待外部主体处理。

## 决策记录

- **ADR-001**：官方任务书是上位依据；第 8 页将测试用例不足 50 组列为方案不完整扣分条件，项目以 `>=50` 作为完整性边界而不虚称为单列满分档。300+ 知识点、双人盲评、10,000 次 case-cluster Bootstrap、保守 CI 和证据状态机是内部加强措施。
- **ADR-002**：模型可以生成内容，确定性代码只计算、验证和渲染；代码不产生人工事实。
- **ADR-003**：正式三指标依赖冻结的独立人工真值；现有规则评测仅用于内部回归和诊断。
- **ADR-004**：断言/适配记录按 case-cluster Bootstrap 10,000 次计算不确定性，并使用 Wilson 与 Bootstrap 的保守包络。
- **ADR-005**：用保守区间边界而不是点估计作谬误率和适配率判定；覆盖率对冻结全集加权计算。
- **ADR-006**：PDF、PPT、视频、盖章报名表、报名/送达回执和正式批准 scorecard 在实际产生前均是外部缺口，不做完成性陈述。

## 最终验收清单

1. 从 G0 矩阵确认所有官方条款都有路径、证据、责任角色、门禁和真实状态。
2. 从 G1 UAT 确认清洁部署、三类画像和反馈再生成存在运行证据。
3. 从 G2 确认独立人工审阅/仲裁证据与签字真实存在，且可复算结果不是 `not_assessable`；仅在 `pass` 时说明该冻结数据的指标门禁通过。
4. 从 G3 确认实际存在的展示材料与唯一候选 SHA、scorecard 一致；不存在则明确未完成。
5. 从 G4 确认包、校验和、报名表和送达回执实际存在；赛事审核和结果继续按外部状态记录。

### UAT-04 精确命令（Windows PowerShell）

在仓库根目录执行。该块刻意以空 provider key 运行，避免本地凭据影响回归结果；模板 smoke 的退出 `2` 是预期值，后续检查会把其他退出码变成失败。

```powershell
$env:AGENTEDU_MINIMAX_API_KEY = ''
$env:AGENTEDU_DEEPSEEK_API_KEY = ''
$env:AGENTEDU_API_KEY = ''
py -3 -X utf8 -m unittest discover -s tests
if ($LASTEXITCODE -ne 0) { throw 'Python full test suite failed' }
node --test tests/frontend-ui.test.mjs
if ($LASTEXITCODE -ne 0) { throw 'frontend UI tests failed' }
$scorecardOut = Join-Path ([System.IO.Path]::GetTempPath()) ('agentedu-scorecard-' + [guid]::NewGuid())
py -3 -X utf8 -m evalkit.formal_scorecard --truth data/evaluation/formal_truth.template.json --out $scorecardOut
if ($LASTEXITCODE -ne 2) { throw 'template must produce not_assessable with exit 2' }
if (-not (Test-Path (Join-Path $scorecardOut 'scorecard.json')) -or -not (Test-Path (Join-Path $scorecardOut 'scorecard.md'))) { throw 'template reports were not written' }
py -3 -X utf8 -m tools.evidence_index --check
if ($LASTEXITCODE -ne 0) { throw 'evidence index check failed' }
```
